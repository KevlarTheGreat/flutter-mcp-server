{
  "name": "flutter-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Flutter development — hot reload, hot restart, logs, device management",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "install-deps": "npm install"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
